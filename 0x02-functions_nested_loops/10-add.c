#include "main.h"

/**
 * add - A function that adds two integers
 * @x: first parameter
 * @y: second parameter
 * Return: 0
 */

int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
